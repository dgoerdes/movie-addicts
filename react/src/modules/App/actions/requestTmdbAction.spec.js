import requestTmdbAction from './requestTmdbAction';

describe('requestTmdbAction', () => {

    it('requestTmdbAction is a function', () => {
        expect(requestTmdbAction).toBeInstanceOf(Function);
    });
});

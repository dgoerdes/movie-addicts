import initMoviesSignal from './initMoviesSignal';

describe('initMoviesSignal', () => {

    it('initMoviesSignal is an array', () => {
        expect(initMoviesSignal).toBeInstanceOf(Array);
    });
});
